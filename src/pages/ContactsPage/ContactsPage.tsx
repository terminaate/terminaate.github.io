import React from 'react';
import PageContainer from '@/components/PageContainer';

const ContactsPage = () => {
  return <PageContainer>Contacts page</PageContainer>;
};

export default ContactsPage;
