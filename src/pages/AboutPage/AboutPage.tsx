import React from 'react';
import PageContainer from '@/components/PageContainer';
import cl from './AboutPage.module.scss';

const AboutPage = () => {
  return <PageContainer className={cl.aboutPage}>
    About me page
  </PageContainer>;
};

export default AboutPage;
